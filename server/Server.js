// var mongo_url=process.env.MONGOHQ_URL;

var mongo_url=process.env.MONGO_URL;

console.log('Connecting to - ' + mongo_url);
console.log(process.env);

//var mongo_url='mongodb://localhost:5001/meteor';

